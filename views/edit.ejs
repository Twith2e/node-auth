<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css" />
    <title>Edit</title>
  </head>
  <body>
    <div class="container mt-5">
      <form
        action="/todo/update/<%= toBeEdited && toBeEdited._id %>?_method=PUT"
        method="post"
        class="mb-3"
      >
        <div class="form-group mb-3">
          <label for="title">Title</label>
          <input
            type="text"
            class="form-control"
            name="title"
            id="title"
            value="<%= toBeEdited && toBeEdited.title %>"
          />
        </div>
        <div class="form-group mb-3">
          <label for="content">Content</label>
          <input
            type="text"
            class="form-control"
            name="content"
            id="content"
            value="<%= toBeEdited && toBeEdited.content %>"
          />
        </div>
        <button type="submit" class="btn btn-primary">Update</button>
      </form>
    </div>

    <script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
